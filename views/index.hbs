<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
</head>
<body>
  <h1>{{title}}</h1>

  <h3>Timestamp: {{timestamp}}</h3>
  <p>
    {{paragraph}}
  </p>

  <ul>
    {{#each colors}}
      <li>{{this.name}}, {{this.shade}}</li>
      <button id={{this.name}}>{{this.name}}</button>
    {{/each}}
  </ul>


</body>
</html>
